# Terraform working directory
.terraform/

# Terraform state files
*.tfstate
*.tfstate.*

# Terraform crash logs
crash.log

# Variable files (may contain secrets)
*.tfvars
*.tfvars.json

# Terraform lock file
# Keep this in production projects, but ignore for this learning lab if desired
# .terraform.lock.hcl